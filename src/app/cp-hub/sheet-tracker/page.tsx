import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sheet Tracker | CP Hub | MEC Computer Club",
};

export default function SheetTrackerPage() {
  return (
    <main className="section container">
      <div className="section-header">
        <span className="kicker">CP Hub</span>
        <h2>Sheet Tracker</h2>
        <p>This page is currently under construction. Check back soon for the progress sheet tracker!</p>
      </div>
    </main>
  );
}
